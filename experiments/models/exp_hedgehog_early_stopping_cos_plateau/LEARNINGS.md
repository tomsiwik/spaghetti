# LEARNINGS — exp_hedgehog_early_stopping_cos_plateau

## Core Finding

KILLED preempt-structural. F#756 registered. K1935 (cos-sim tightness within 0.02) and K1936 (training-time savings ≥30%) are both proxies; no target-metric KC ⇒ F#666-pure standalone, no admissible verdict in any of the 4 PASS/FAIL outcome cells. ~26th drain-window F#666-pure-standalone instance; 7th Hedgehog-ablation super-family sub-type (saturation at 7); 3rd cos-sim-bucket form (tightness/distance, after F#720 final-value, F#755 convergence-speed); 1st training-axis efficiency-bucket form (adjacent to F#753 inference-axis routing-latency); 2nd cos-sim-on-cos-sim circularity (intra-training-trajectory plateau-driving-eval — tighter than F#755's notes-level ordering-driving-eval).

## Why

(1) Cos-sim tightness is doubly-proxy: r≈0.08 PPL→behavior (guardrail 1006) + bound-on-output ≠ bound-on-behavior (Caruana-Lawrence-Giles 2000 NeurIPS early-stopping-as-regularization decoupling: full-trained adapter can be over-fit while early-stopped is behaviorally superior despite cos-sim divergence). (2) Training-time savings is a unitless ratio with no behavioral pairing — iter=0 saves 100% compute and produces a noise adapter; KC alone can't distinguish honest from useless savings. (3) Stopping signal IS evaluation signal (cos-sim plateau halts training on a cos-sim equivalence test), tighter circularity than F#755 — cannot disambiguate plateau-detection sensitivity from behavioral fitness. (4) Implicit parent F#683 (Hedgehog politeness) still PROVISIONAL — child-on-unverified-parent risk per F#669 family even if target-pair were added.

## Implications for Next Experiment

Hedgehog-ablation super-family is now SATURATED at 7 sub-types. Avoid claiming any further Hedgehog-ablation pre-reg (axis-extension, loss-variant, layer-selection, hyperparameter, data-augmentation, curriculum-ordering, training-stopping-criterion) without explicit target-pair design per F#723 template. Avoid routing-acc and infrastructure-benchmark sub-flavors unless paired with task-quality target. Routinely close F#666-pure standalone pre-regs as structurally-malformed; recommend re-register `_behavioral` variant gated on F#683 → SUPPORTED with: held-out validation loss as stopping signal (Caruana 2000 — DECOUPLED from eval), Prechelt 1998 PQ_α plateau threshold (anchored), LLM-judge politeness on F#683 prompt set + Hacohen-Weinshall 2019 (arxiv:1904.03626) generalization-gap on hard-tail prompts as target KCs. Next claim candidates with target-anchored notes: similarity_across_seeds, jepa_scale_sweep_5m_15m_50m, lora_rank_importance_per_task, init_comparison_v2, cross_axis_interference, fingerprint_uniqueness, hotswap_latency_impl. Recommend researcher prefer claims whose pre-reg notes already commit to a named target-metric KC and explicit references list.
