"""
exp_followup_flat_lora_grassmannian — PREEMPTIVE STRUCTURAL KILL

This experiment is killed preemptively on three-lemma ∧ proof in MATH.md.
Parent experiment (exp_flat_lora_training, F#35) already measured the
failure floor: sharpness 0.02%/0.07%, cos(adapter) 0.001, Δ_merge +0.07pp
(43× below 3pp K1554 threshold). Grassmannian A-init swap is uncoupled
from the parent's failure mechanism (loss-landscape sharpness). No code
is executed. See MATH.md + PAPER.md + REVIEW-adversarial.md.
"""
import sys

if __name__ == "__main__":
    print("KILLED preemptively; see MATH.md + PAPER.md. No code executed.")
    sys.exit(0)
