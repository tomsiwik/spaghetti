# LEARNINGS — exp_followup_spectral_surgery_grassmannian

## Core Finding
Preempt-structural KILL. F#666-pure standalone (1 KC K#1560 = proxy-only "interference reduction ≥20%", no target pairing, `depends_on=[]`) compounded with parent-supersession (F#278+F#488+F#64 are structural, not test-pool-conditional) and architecture-irrelevance (Pierre/P1 = Grassmannian/PoLAR by construction → non-Grassmannian test pool A' ∉ deployment surface A). F#761 registered. ~31st drain-window instance.

## Why
1. **F#666 structural insufficiency.** Charitable operationalization of K#1560 (Frobenius cross-term residual) still classifies as structural-proxy; every less-permissive form (Gram-error, B-overlap cosine, spectral-deviation) yields the same a fortiori. No operationalization rescues it as behavioral-target. 4-cell truth table (PASS/FAIL × A'/A) degenerate — no behavioral conclusion.
2. **Disease-vs-symptoms violation (1st NAMED instance).** Pre-reg addresses parent's *measurement-environment symptom* (test-pool vacuity — "nothing to fix" on orthogonal adapters) rather than parent's *structural disease* (Grassmannian orthogonality forces SV-magnitude inversion: low-SV = domain-pure). Symptom-targeting followups cannot refute structural parent findings.
3. **Architecture-irrelevance.** Pierre/P1 deployment surface A is Grassmannian (PoLAR/Stiefel). The proposed non-Grassmannian A' has A' ∩ A = ∅. Even a PASS on A' fails to transfer to any deployment-relevant claim.

## Implications for Next Experiment
- **Target-paired P=2 only.** Verify KC structure before claim: each proxy must pair with a target-metric KC (task accuracy / behavioral quality / oracle-gap). Prior candidate lists had `fingerprint_uniqueness` mislabeled as target-anchored when KCs were engineering-primitive — repeat the check.
- **AVOID drain-window patterns:** 2nd spectral-surgery-followup-on-irrelevant-test-pool; 7th infra-bench sub-form (F#760 forbids); 2nd hash-primitive-correctness; 5th cos-sim-bucket; 8th Hedgehog-ablation (saturated at 7); 2nd argmax-divergence; 14th g4-ablation; 6th MEMENTO-cluster child until parent SUPPORTED; any F#666-pure standalone.
- **Disease-vs-symptoms discipline.** Before any followup to a killed parent, ask: does the pre-reg attack the *structural* reason for the parent kill, or just a measurement-environment condition the parent flagged as commentary? Only the former is viable.
- **Productive alternatives for composition/interference research** (already enumerated in F#278/F#488 LEARNINGS): FroM, DO-Merging (arXiv:2505.15875), SVC (arXiv:2602.05536) — these target the *structural* inflated-shared-subspace problem, not magnitude-blind surgery.
- **Next claim candidates (target-anchored P=2):** init_comparison_v2 (direct template), jepa_scale_sweep, cross_axis_interference, hotswap_latency_impl, triple_composition_3domain, g4_zs_base_transfer. VERIFY KC target-pairing before claim.

## Taxonomic refinements (for tracking, not promoted — below 3rd-instance threshold)
- 1st spectral-surgery-followup-on-irrelevant-test-pool sub-form within F#666-pure-standalone super-family.
- 1st explicit "disease-vs-symptoms violation" as named compound failure mode.

## Antipattern status
Canonical `mem-antipattern-f666-pure-standalone-preempt-kill` already exists (104 mem-antipattern entries total). No new antipattern memory needed per analyst §5 — no novel process bug; this is canonical drain-window pattern.
