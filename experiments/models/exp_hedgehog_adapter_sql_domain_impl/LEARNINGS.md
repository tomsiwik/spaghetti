# LEARNINGS.md — exp_hedgehog_adapter_sql_domain_impl

**Verdict:** PROVISIONAL — F#768 model-cache sub-form (no new finding registered
per F#769 ledger-explosion closing-note).

## Core Finding

The F#768/F#769 BLOCKED-on-resource super-family generalizes from macro-scope
PROVISIONAL filings (knowledge_gap_26b, long_context) to micro-scope `_impl`
cohort filings. When N sibling experiments share a single resource blocker, the
correct unit of work is **one cohort-level runtime task**, not N individual
finding numbers. Filed `task: cache:26b-gemma4-teacher` (P=2) which unblocks
4 hedgehog `_impl` siblings (JS/Python/Rust/SQL) plus the macro
`exp_model_knowledge_gap_26b_base` simultaneously.

## Why

- **F#666-pair non-tautological:** K1957 PPL proxy + K1958 PostgreSQL EXPLAIN
  dual-ground-truth target preserved untouched. KILL on monotonic prior alone
  would discard the novel-structure signal of the 4-domain-axis cohort.
- **F#702 hygiene-patch path AVAILABLE:** K1958 is target (not F#666-pure-saturated).
- **Refusal scaffold is canonical:** No silent E4B-as-teacher substitution
  (antipattern m); capacity-degeneracy argument for cos-sim distillation requires
  teacher capacity > student.
- **Doom-loop check passed:** 3rd consec PROVISIONAL but on 3 orthogonal mechanisms
  (scaling-law / range-extrapolation / per-layer cos-sim distillation). Cohort-drain
  shape, not researcher-loop pathology. `python3 .ralph/tools/doom_loop.py` exit 0.

## Implications for Next Experiment

1. **Batch-defer the 3 sibling hedgehog _impls** (JS/Python/Rust @ P=3) until
   `cache:26b-gemma4-teacher` task lands. Re-claiming any of them this drain cycle
   would file an identical PROVISIONAL with no new content — pure ledger churn.
2. **Next claim should target a P≤2 open with no shared resource blocker** OR a
   structurally different verdict path. A 4th consecutive PROVISIONAL escalation
   without intervening drain-scope progress fires the doom-loop signal.
3. **F#770 is NOT for further BLOCKED-on-resource cohort siblings.** Reserve a
   new finding number only if a 5th case arrives with a novel sub-form (neither
   model-cache nor compute-budget).
4. **When the cache lands**, single dedicated session executes parent
   `exp_hedgehog_adapter_sql_domain` §6 verbatim — no redesign needed, KCs locked.
