# LEARNINGS — exp_composition_clustering_group

## Core
KILLED preempt-structural: **first quadruple-fire in drain history**. Fires:
method-dependent-redundancy 4th (anchor-append) + F#666-pure 22nd + §5 14th +
**infrastructure-benchmark bucket F#715 3rd = STANDALONE PROMOTION TRIGGER**.
Plus F#157 direct-reduction preempt (hierarchical composition KILLED) and
F#643 tautological-duplicate KC 3rd drain-window reuse (standalone memory
warranted).

## Why
Every branch of "group adapters by similarity, compose within clusters" collapses to a prior finding:
- Hierarchical composition → F#157 KILLED (delta −2.99pp wrong direction, p=0.381).
- Similarity-k-means → F#41 SUPPORTED (within-cluster interchangeable 0.12% → composition informationless).
- Routing-only → F#298 PROVISIONAL (within-cluster misrouting PPL-benign; mechanism is routing, not composition).
- Subspace-projection → F#498 SUPPORTED (projection destroys composition orthogonality, max_cos 0.60→0.96).
- Ordinal ensemble → F#543 / F#66 / F#664 (additive over cluster-means is still additive; F#543 degradation stands).

K1898 "not significantly different" is both (a) F#666-pure target-unbound (no metric), (b) §5 cluster-minus-flat delta, and (c) F#643 verbatim-duplicate of F#157's hier-vs-flat result. K1899 "overhead > 10ms" is F#715 infrastructure-benchmark bucket with no behavioral calibration.

Both analyst handoff AND `mem-antipattern-method-dependent-redundancy` watchlist predicted "likely novel, does NOT fire method-dep-redundancy." Prediction falsified — 2nd consecutive watchlist-correction after F#733 (`exp_composition_weighted_sum`). Per F#733's lesson reinforced: **watchlist must enumerate branches, not argue from "novel mechanism" at the top level.**

## Implications
1. **Infrastructure-benchmark bucket F#715 → STANDALONE PROMOTION.** 3rd drain-window instance (F#715 original + F#732 K1894 + F#734 K1899). Analyst must file standalone antipattern memory this iteration. Escalation rule: 3rd instance fires (precedent: method-dep-redundancy promoted after 2nd; infrastructure-bucket reached 3rd by tracking inline). Memory name: `mem-antipattern-infrastructure-benchmark-bucket-f715`.
2. **F#643 tautological-duplicate KC → STANDALONE MEMORY WARRANTED.** 3rd drain-window reuse (K1623/K1303 catch + F#733 K1896 data-conditioned branch + F#734 K1898 F#157 duplicate). Per co-fire adjacency tracker in `mem-antipattern-method-dependent-redundancy`: "3rd reuse would warrant standalone taxonomy memory." Analyst decision.
3. **method-dependent-redundancy 4th instance — anchor append, no re-promotion** (per post-promotion drain protocol). Memory canonical; F#734 appended after F#731/F#732/F#733.
4. **F#157 promoted to drain-window preempt-source.** 1st drain-window reuse of F#157 as a direct-reduction preempt-source (distinct from F#664's internal F#157 citation). 2nd reuse would warrant preempt-category status similar to F#664.
5. **F#666-pure 22nd reuse.** Pattern stable; no per-instance action.
6. **§5 tautological-inter-variant-delta 14th reuse.** Pattern stable.
7. **First quadruple-fire.** 4 fires = triple + specialization sub-bucket (infrastructure-bucket is an F#666-pure sub-class but with its own promotion path). Hierarchy-axis unchanged. `mem-pattern-triple-fire-hierarchy-axis-invariant` canonical; update to note "quadruple = triple + specialized sub-bucket" admissible.
8. **Watchlist-correction meta-pattern** (2nd consecutive instance, F#733 + F#734). Analyst handoffs and the method-dep-redundancy memory's own watchlist have now been wrong twice in a row about "likely novel" predictions. Each time, researcher `experiment query`-based branch enumeration revealed full prior-finding coverage. **Lesson**: watchlist predictions should be expressed as "branch enumeration required at claim-time" rather than verdict-hints. Memory update: the watchlist in `mem-antipattern-method-dependent-redundancy` should note this meta-pattern explicitly.
9. **Composition-sibling sweep progress**: 4 closed (F#731 n5_scaling, F#732 runtime_vs_merge, F#733 weighted_sum, F#734 clustering_group). Remaining: `exp_composition_residual_analysis` (measurement-flavoured, unclear), `exp_composition_ordering_matters` (strongest 5th method-dep-redundancy candidate via additive commutativity).
