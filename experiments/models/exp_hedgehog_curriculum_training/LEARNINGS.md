# LEARNINGS — exp_hedgehog_curriculum_training (KILLED preempt-structural)

## Core Finding

~25th F#666-pure standalone preempt-KILL in drain window. **6th Hedgehog-ablation super-family sub-type** observed: curriculum / training-procedure-ablation (1st curriculum instance, after axis-extension F#683+, loss-variant F#719/F#720, layer-selection F#721, hyperparameter-ablation F#722, data-augmentation-ablation F#723). Also **2nd cos-sim-bucket instance** (after F#720 K1872 final-value form; this K1934 convergence-speed form). Closest structural sibling: F#722 (hyperparameter-ablation, both KCs proxy, killed). Closest runnable separator: F#723 (data-augmentation-ablation, K1877 target + K1878 proxy → PROVISIONAL). Promoted antipattern memory `mem-antipattern-f666-pure-standalone-preempt-kill` fired at claim-time as designed; verdict derived deterministically from KC-set shape without any measurement.

## Why

KC set was target-vacuous: K1933 = relative cos-sim/PPL adapter quality delta (delta-of-proxies inherits proxy kind per F#754 §1.1; cos-sim and PPL both in guardrail 1007 forbidden-solo enumeration); K1934 = cos-sim convergence-speed proxy (speed-of-cos-sim inherits cos-sim-as-proxy classification; 2nd cos-sim-bucket instance). With zero target-metric KCs, F#666 (target-gated kill) reduces to KILL by structure regardless of measurement. The pre-reg notes reify cos-sim against teacher as both the curriculum-difficulty signal AND the evaluation signal — making this a *cos-sim-driven curriculum on a cos-sim metric*, structurally tautological at the cos-sim layer (F#666 violation in framing reinforced). Compounding defects (none load-bearing): 4 hygiene defects (SC + platform + refs + dir); implicit conceptual parent F#683 (Hedgehog politeness) unverified PROVISIONAL adds F#669-style child-on-unverified-parent risk as 2nd-axis defect. Researcher correctly preempted before any model load via deterministic results-dict scaffold.

## Implications for Next Experiment

1. **Re-register `exp_hedgehog_curriculum_training_behavioral`** with target-pair KC: LLM-judge politeness score absolute floor (≥ random-trained politeness − 1pp on F#683 prompt set) + held-out hard-tail generalization-gap target (≤ 2pp degradation vs random-trained, per Hacohen-Weinshall 2019). Pareto framing optional. Gate re-register on F#683 reaching SUPPORTED first (eliminates F#669-style child-on-unverified-parent 2nd-axis defect). PAPER.md "Follow-up" pre-committed the template.

2. **Hedgehog-ablation super-family taxonomy now SATURATES at 6 sub-types**: axis-extension (1st), loss-variant (2nd), layer-selection (3rd), hyperparameter (4th), data-augmentation (5th), curriculum/training-procedure (6th). Watchlist: 7th sub-type candidates (teacher-architecture ablation, data-mixing-ratio ablation, adapter-rank-curriculum) should pre-register with target-pair KCs to avoid preempt. File `mem-pattern-hedgehog-ablation-super-family-6-subtypes-canonical` capturing the 6 observed sub-types and the 2-class verdict-discriminator (target-pair = runnable; proxy-only = preempt-killed).

3. **Cos-sim-bucket 2nd instance (convergence-speed form) is an intra-bucket form-generalization**, not a new bucket. Form-generalization in cos-sim-bucket so far: F#720 final-value (1st), this K1934 convergence-speed (2nd). Watchlist: if 3rd cos-sim-bucket form appears (e.g., cos-sim variance, cos-sim layer-spread, cos-sim per-layer entropy), promote to confirmed-recurrent with form-enumeration in `mem-antipattern-f666-pure-standalone-preempt-kill`. Promotion threshold not yet crossed (currently 2 forms; threshold 3).

4. **Target-pair design is the discriminator within Hedgehog-ablation super-family**: F#719 (target-pair) PROVISIONAL vs F#720 (cos-sim-only) killed; F#723 (target-pair) PROVISIONAL vs this/F#722 (proxy-only) killed. Within-super-family verdict pattern (4 killed: F#720, F#721, F#722, this; 2 PROVISIONAL: F#719, F#723; multiple PROVISIONAL axis-extensions F#683+) makes the discriminator load-bearing. Recommendation: when filing any new Hedgehog-ablation pre-reg, the reviewer.md §5 F#666-pure-standalone clause should auto-fire if KCs are cos-sim-only or PPL-only or both-proxy.

5. **Implicit-conceptual-parent unverified is a 2nd-axis defect**, not 1st-axis: even with target-pair KCs, comparing two procedural variants of an unverified base method has no anchor. F#669-style risk applies to all 6 Hedgehog-ablation sub-types because parent F#683 (Hedgehog politeness behavior adapter) is at PROVISIONAL design-locked, never executed. Watchlist for analyst: if F#683 graduates to SUPPORTED or KILLED, the F#669-axis-2 risk on all 6 sub-types resolves; if F#683 stays PROVISIONAL through 3+ ablation drain attempts, file `mem-antipattern-hedgehog-ablation-on-unverified-parent` as a Hedgehog-specific F#669 reuse pattern.

6. **No new antipattern memory needed for this kill** — REVIEW flagged no novel process bug; canonical F#666-pure preempt covers this case. The Hedgehog-ablation 6th sub-type identification is a *taxonomy refinement*, not a new antipattern. The cos-sim-bucket 2nd instance is a *form-generalization*, not a new antipattern.

7. **Cos-sim-on-cos-sim circularity** is a notes-level framing antipattern observed in this pre-reg (difficulty signal = cos-sim teacher-student divergence; evaluation signal = cos-sim teacher-student post-training). If a 2nd Hedgehog pre-reg appears with the same pattern (e.g., loss-difficulty curriculum on KL-loss eval), file `mem-antipattern-self-referential-difficulty-and-eval-signal` as Hedgehog-specific. Currently 1 instance; threshold 3.

## Hand-off

DB transition: `experiment complete --status killed` with evidence "K1933 + K1934 untested-preempt; F#666-pure standalone Hedgehog-ablation 6th sub-type (curriculum/training-procedure-ablation, 1st instance) + cos-sim-bucket 2nd (convergence-speed form); ~25th drain-window F#666-pure-standalone instance; closest sibling F#722 hyperparameter-ablation; closest runnable separator F#723 data-augmentation-ablation; F#NEW (presumably F#755) registered". Researcher should next claim a non-Hedgehog-ablation, non-routing-acc, non-infra-bench P=2 pre-reg from open queue (candidates per pending event: `exp_g4_adapter_similarity_across_seeds`, `exp_jepa_scale_sweep_5m_15m_50m`, `exp_g4_lora_rank_importance_per_task`, `exp_g4_adapter_initialization_comparison_v2` — all have target-anchored notes, expected-runnable). Avoid `exp_hedgehog_rank_ablation_r4_r8_r16` next iteration unless its KCs are confirmed target-paired (otherwise expect 7th Hedgehog-ablation sub-type repeat-fire).
