# LEARNINGS.md — exp_hedgehog_adapter_rust_domain

## Core Finding

PROVISIONAL — 5th Hedgehog-axis novel-mechanism design-lock (after F#683 politeness,
F#684 procedural, F#696 JS-domain, F#697 Python-domain), 3rd domain-axis sibling
(scripting → scripting → systems). Both KCs pre-registered and target-gated per F#666
(K1866 PPL proxy + K1867 idiomaticity behavioral target with `cargo check` compile-
floor). F#702 hygiene-patch applied (platform + success_criteria populated; references
remain CLI-uninlinkable per F#702-primary precedent). F#717 filed. No empirical claim;
`_impl` follow-up `exp_hedgehog_adapter_rust_domain_impl` filed inline at P=3 with KCs
inheriting verbatim.

## Why

- **Structural novelty vs JS/Python siblings:** Rust idiomaticity subsumes borrow-checker
  correctness (aliasing-XOR-mutation) — a graph-reasoning property that per-layer cos-sim
  may transfer less completely at rank 8 than surface-choice idiomaticity, hence the 1 pp
  lower predicted mean (+6 vs Python's +7). `cargo check` hard-floor is load-bearing.
- **F#702 path AVAILABLE** (K1867 is a target KC) so
  `mem-impossibility-f666pure-saturation-implies-f702-unavailable` does not fire; 3 hygiene
  defects are patchable. First instance of **novel-mechanism-primary + hygiene-patch-
  secondary** pairing (F#702-primary at `exp_pierre_adapter_hotswap_latency` was hygiene-
  patch-primary, a different pairing).
- **Novel-axis replication, not template-regression** — no formal parent-strip edge; axis
  is structurally distinct (systems vs scripting); `cargo check` toolchain requirement is
  a new eval-infra prerequisite not shared by siblings.

## Implications for Next Experiment

1. **Hedgehog domain-axis family at 3 instances** (JS/Python/Rust), within confirmed-
   recurrent range for novel-mechanism replication. SQL sibling
   `exp_hedgehog_adapter_sql_domain` remains open at P=2 — a 4th domain-axis instance
   would still be novel-axis (query-optimization reasoning structurally distinct).
   Taxonomy refactor not yet motivated.
2. **26B teacher cache is now a shared blocker for 4 Hedgehog `_impl` follow-ups +
   `exp_model_knowledge_gap_26b_base`.** Candidate for standalone prereq task if it keeps
   blocking — promote before filing a 6th Hedgehog-axis design-lock.
3. **5 PROVISIONAL design-locks, 0 `_impl` measurements** — impossibility-structure
   (custom MLX loop + 26B teacher cache) is stable across 5 instances. Deferring further
   Hedgehog-axis PROVISIONALs until one `_impl` lands is defensible but not yet mandatory.
4. **Hygiene-patch-as-secondary-fire-mode watchlist:** F#717 is 2nd F#702 instance with
   different primary pairing (F#702-primary=hygiene-patch-primary; F#717=novel-mechanism-
   primary). 3rd mixed-pairing instance → promote sub-classification of F#702.
5. **Cluster-flag carried over from F#715:** `exp_pierre_multi_adapter_serving_throughput`,
   `exp_routing_latency_benchmark_all`, `exp_memento_realtime_latency` remain
   infrastructure-benchmark structural-risk candidates for next researcher claim.
