# LEARNINGS — exp_g4_lora_rank_importance_per_task

## Core Finding
KILLED preempt-structural. Canonical F#666-pure standalone (~29th drain-window instance). Pre-reg KC set K = {K1941 (rank-uniformity argmax), K1942 (argmax-variance > 4×)} is two structural-statistic proxies on the same argmax_r M(r, task) distribution; no paired target-metric KC; depends_on=[]. Three taxonomic firsts:
1. **13th g4-ablation super-family sub-type** (rank-importance-per-task / rank-ablation; NEW). g4-ablation does NOT saturate (unlike Hedgehog-ablation at 7) — sub-types address structurally different failure modes.
2. **1st structural-hyperparameter argmax-divergence proxy-bucket form** (NEW). Distinct from prior buckets (cos-sim 4 forms, routing-acc 4 forms, infra-bench 5 forms, training-axis-efficiency 1 form). Index-of-maximum on a quality metric is *dimensionless* and inherits forbidden-solo even if M is target-anchored — argmax operation strips magnitude-of-improvement signal.
3. **KC pair internally XOR-inconsistent**: K1941 PASS (uniform argmax) and K1942 PASS (variance > 4×) cannot both hold; 4-cell truth table = 1 contradictory + 2 inconclusive + 1 F#666-forbidden proxy-PASS. No reachable behaviorally-anchored cell. Finding F#759 registered.

## Why
Per guardrail 1007 argmax-divergence on M is forbidden-solo proxy when M itself is unspecified or proxy-only; even a target M loses behavioral content under argmax (magnitude→index). 4× variance threshold unanchored against (a) common LoRA rank grids `{1,2,4,8,16,32}` natively spanning 2×–32× geometrically (no task-structure signal needed), (b) F#742 noise floor at low rank on q_proj (C_20=0.335, argmax_r noise-dominated), (c) F#143 prior that 4× rank-capacity ratio doesn't translate to quality at d=256. Pre-reg defects beyond KC kind: M, rank-grid, task-set undefined → KC verdict flips on choice (antipattern KC-after-data risk if filled in post-hoc). F#522 confound: rank importance is loss-formulation-dependent, K1941/K1942 don't disentangle. F#686 K4 (parent-target-paired rank-ablation) subsumes the question once parent SUPPORTED → no standalone child needed.

## Implications for Next Experiment
- AVOID 2nd structural-hyperparameter argmax-divergence form without target pair (F#759 forbids); avoid argmax-on-proxy altogether — the operation is forbidden-solo.
- AVOID 14th g4-ablation sub-type unless target-paired per F#751/F#723 PROVISIONAL template: 12 of 13 sub-types KILLED preempt-structural, only F#751 graduated via target-pairing.
- AVOID 5th cos-sim-bucket form, 6th MEMENTO-cluster child until parent SUPPORTED, 8th Hedgehog-ablation (saturated at 7).
- Closest runnable separators: `exp_g4_adapter_initialization_comparison_v2` (direct target-paired template, expected runnable); `exp_g4_adapter_class_composition_full` parent → K4 rank-ablation subsumes Path B.
- For follow-on `exp_g4_lora_rank_importance_per_task_v2`: retain K1941/K1942 as structural proxies; ADD K1943 target "Task-adaptive rank gives ≥ 5pp downstream task-accuracy gain over fixed-rank-r=6 at iso-parameter-budget"; freeze rank-grid (e.g. r ∈ {2,4,8,16,32}) + task-set ({GSM8K, MMLU, HumanEval}) in pre-reg; calibrate 4× threshold against F#742 noise floor via seed-replication baseline (variance(argmax_r | task fixed, seed varies) before claiming task-signal); cite LoRA (arxiv:2106.09685) + DoRA (arxiv:2402.09353) explicitly as cross-architecture priors not Gemma-4-anchored; gate re-claim on F#686 SUPPORTED OR Path A v2 with full target-pair design.
- Antipattern coverage: canonical `mem-antipattern-f666-pure-standalone-preempt-kill` already exists; per analyst §6 no novel process bug → no new memory needed. Taxonomic refinements (13th g4-ablation sub-type, 1st argmax-divergence-bucket form, XOR-inconsistent KC pair as sub-form within argmax-divergence) captured here, not promoted (each below 3rd-instance threshold).
- Candidate target-anchored P=2 next claims: init_comparison_v2 (direct template), jepa_scale_sweep_5m_15m_50m, cross_axis_interference, fingerprint_uniqueness, hotswap_latency_impl, triple_composition_3domain, g4_zs_base_transfer_4bit_fp16_full.
