# LEARNINGS — exp_g4_adapter_similarity_across_seeds

## Core Finding
KILLED preempt-structural. Canonical F#666-pure standalone (~27th drain-window instance). Pre-reg KC set K = {K1937 (cos > 0.80), K1938 (cos < 0.30)} is two cos-sim proxy thresholds on the same cross-instance pairwise cos-sim population statistic, no paired target-metric KC. Three taxonomic firsts:
1. **1st g4-ablation seed-determinism / reproducibility-within-init-recipe sub-type** (g4-ablation taxonomy now at 12 sub-types).
2. **4th cos-sim-bucket form**: cross-instance population-statistic with dual-tail thresholds (after F#720 final-value, F#755 convergence-speed, F#756 tightness/distance).
3. **1st no-verdict-cell-by-construction precedent in cos-sim-bucket**: dual-tail design admits 3-cell verdict map (deterministic 0.80+ / intermediate 0.30–0.80 / seed-dep 0.30-); intermediate cell fires neither KC. Finding F#757 registered.

## Why
Per guardrail 1007 cos-sim is forbidden-solo proxy. Per F#666 dual-tail truth table all three cells are inadmissible: deterministic-tail PASS is tautological "task determines adapter" without behavioral evidence; intermediate band produces no verdict; seed-dependent-tail PASS is tautological "seed determines adapter" — F#666 canonical decoupling (40.2% per-sample acc + 0.0% target gap) shows cluster-equivalence at decision boundaries can preserve behavior despite parameter-space orthogonality (or break behavior despite parameter-space tightness). Thresholds (0.80, 0.30) unanchored to F#562 baseline (cos≈4.77e-9) and F#751 final-cos (0.977–0.9995), making intermediate band likely never fire — dual-tail collapses to arbitrary single-tail in disguise. Hygiene-defect count = 8 (≥3 → AP-prereg-hygiene-multi-defect), but F#666-pure structural defect alone is sufficient. Sibling `exp_g4_adapter_initialization_comparison_v2` (open at P=2) addresses overlapping ground with target-paired KC structure (K1977 cos proxy + K1978 PPL-ratio target + K1979 within-init seed-PPL-variance target); v2 K1979 directly answers the seed-determinism question in PPL terms.

## Implications for Next Experiment
- AVOID 5th cos-sim-bucket form (e.g. temporal cos-sim drift, cos-sim conditional on input distribution) without target-pair: 4 forms saturated; auto-preempt likely.
- AVOID further g4-ablation sub-types unless target-paired per F#751 PROVISIONAL template: 12 sub-types covered, 11 KILLED preempt-structural, only F#751 graduated via target-pairing.
- Closest runnable separator: `exp_g4_adapter_initialization_comparison_v2` (target-paired; runnable). Claim it next; gate any reproducibility-axis re-registration on its result.
- For follow-on `exp_g4_adapter_similarity_across_seeds_behavioral`: target-pair cos-sim with PPL-spread (F#751 v2 K1978/K1979 pattern) + behavioral-equivalence (LLM-judge / rouge-L on N=20 medical prompts) + Pareto-quality target; anchor cos threshold to F#562 baseline (>0.95); use distinct top-level `mx.random.key(seed_i)` per F#750 (NOT split-of-shared-key); cite Bouthillier 2019 (arxiv:1909.10314 reproducibility framework) + Madaan 2024 (arxiv:2402.01906 LoRA cross-seed task-accuracy stability) + F#169/F#562/F#627/F#750/F#751 v1+v2; gate on v2 SUPPORTED.
- Antipattern coverage: canonical `mem-antipattern-f666-pure-standalone-preempt-kill` already exists; per analyst §6 no novel process bug → no new memory. Taxonomic refinements (4th cos-sim-bucket form, 1st no-verdict-cell-by-construction sub-form, 1st g4-ablation seed-determinism sub-type) captured here, not promoted (each below 3rd-instance threshold).
- Candidate target-anchored P=2 next claims: init_comparison_v2 (direct template), jepa_scale_sweep_5m_15m_50m, lora_rank_importance_per_task, cross_axis_interference, fingerprint_uniqueness, hotswap_latency_impl, triple_composition_3domain.
