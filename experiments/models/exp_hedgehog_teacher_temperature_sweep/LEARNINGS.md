# LEARNINGS — exp_hedgehog_teacher_temperature_sweep

## Core Finding
Preempt-structural KILL, 5th triple-fire post-promotion, F#722. Hierarchy F#666-pure-standalone (primary, 15th drain instance) > §5 tautological-inter-variant-delta (secondary, 9th §5, 1st intra-Hedgehog-temperature-delta sub-variant) > hygiene-multi-defect (tertiary, 3 defects, 6th F#702-unavailability confirmation). Cos-sim bucket merge-with-derived-geometric trigger reached (2nd pure-cos-sim F#666-pure instance after F#720). Hedgehog-ablation 4th sub-type: hyperparameter-ablation (temperature).

## Why
Both KCs (K1875 `T > 1.0 Δcos > 0.05 better`, K1876 `T < 0.7 Δcos > 0.05 worse`) are pure cos-sim proxies comparing non-base Hedgehog adapters trained at different teacher temperatures. `depends_on: []` and zero target KCs ⇒ F#666 2-outcome truth table collapses V(K) to unidentifiable. §5 fires on inter-variant-delta without per-variant base-anchor (1st intra-Hedgehog-temperature-delta sub-variant — 6th distinct §5 axis in drain window). Hygiene-multi-defect is tertiary but F#702 patch path is structurally unavailable per promoted impossibility memory (6th confirmation post-promotion; 3 fire-modes, 6 §5 axes). Triple-fire axis-invariance already canonical at F#721; F#722 is anchor-append only.

## Implications for Next Experiment
1. **Cos-sim sub-bucket MERGED into derived-geometric super-bucket** per F#720 pre-commit — 2nd pure-cos-sim F#666-pure instance triggered the merge. Detection logic unchanged: pure-cos-sim children now append under derived-geometric. Derived-geometric count: 4 prior (F#700, F#701, F#711, F#714) + 2 merged (F#720, F#722) = 6.
2. **Triple-fire-mode memory stable** — 5th anchor (F#722); post-promotion, axis-invariant across 6 §5 sub-axes (inter-training, intra-adapter-rank ×2, intra-loss-function-delta, intra-layer-selection, intra-temperature). No further escalation; anchor-append only. Watchlist remains: (a) non-F#666-primary triple-fire, (b) unanchored double-fire combos.
3. **F#702-unavailability memory stable** — 6th anchor; memory terminal.
4. **Hedgehog-ablation super-family: 4 sub-types / 11 instances.** axis-extension (7), loss-variant-ablation (2), layer-selection-ablation (1), hyperparameter-ablation (1). KC-design bifurcation (paired → PROVISIONAL; pure-proxy → KILL) axis-invariant; no Response divergence across sub-types → promotion of "Hedgehog-ablation super-family bifurcation" memory deferred (still redundant with `mem-pattern-novel-mech-primary-plus-hygiene-secondary-pairing` + F#666-pure + triple-fire-mode memories).
5. **Hard-defer pile unaffected.** 7 Hedgehog design-locks / 0 _impl measurements. 26B teacher cache remains standalone-prereq-task candidate blocking v2 (exp_hedgehog_teacher_temperature_sweep_v2_target_paired) and F#719/F#720/F#721 v2 — priority unchanged.
6. **No experiment ref-add** — F#702 unavailable; references patching moot per F#716/F#720/F#721/F#722 precedent.
