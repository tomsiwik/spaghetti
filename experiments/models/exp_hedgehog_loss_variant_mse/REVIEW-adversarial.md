# REVIEW-adversarial — exp_hedgehog_loss_variant_mse

**Verdict:** KILL (preempt-structural, 3rd triple-fire precedent)

**Reason:** TRIPLE-FIRE preempt-structural KILL per MATH.md L1–L5.
- **Primary:** F#666-pure-standalone (13th drain-window instance, 1st cos-sim bucket → opens 6th bucket)
- **Secondary:** §5 tautological-inter-variant-delta (7th §5 instance, 1st `intra-loss-function-delta` sub-variant)
- **Tertiary:** hygiene-multi-defect (3 defects), F#702 patch path **unavailable** per promoted impossibility memory `mem-impossibility-f666pure-saturation-implies-f702-unavailable` (4th post-promotion confirmation)

## Preempt-structural (F#666-pure-standalone clause) — required pattern ✅
1. **MATH.md §1 theorem** — L1 gives the canonical 2-outcome F#666 truth table on sole KC K1872: PASS = tautological-support on pure geometric similarity, FAIL = "finding about the proxy, not a kill"; both unidentifiable. Cites F#666. ✅
2. **run_experiment.py graceful-failure** — imports only `json` + `pathlib`; `main()` never raises; writes `results.json` with `verdict="KILLED"`, K1872 `result="preempted"`, `preempt_structural=true`. ✅
3. **PAPER.md** — verdict line "KILLED (preempt-structural, pre-measurement)"; 5-row prediction-vs-measurement table (all "Not measured (preempt)"); explicit **Unblock path** section (v2 target-paired pre-reg reusing F#683 politeness corpus). ✅
4. **No `_impl` companion** — correct per clause; unblock is pre-reg-external. ✅

## Adversarial checklist
- **(a)** results.json verdict=KILLED ↔ DB status=killed. ✅
- **(b)** `all_pass=false`; consistent with killed. ✅
- **(c)** PAPER.md verdict line "KILLED". ✅
- **(d)** `is_smoke=false`; preempt path, not smoke. ✅
- **(e)** K1872 unchanged since pre-reg. ✅
- **(f)** Tautology sniff: K1872 IS tautological-inter-variant-delta — disclosed as the §L2 reason for the kill. Preempt-structural → tautology is the *reason*, not concealment. ✅
- **(g)** K1872 text matches DB. ✅
- **(h–l)** No MLX code runs; no composition / scale / routing / copy / hardcoded-pass. ✅
- **(m)** No model load (`no_model_loaded=true`). ✅
- **(m2)** **Carve-out applies** per F#716 precedent — preempt-structural, no MLX. MATH.md §0 cites the carve-out explicitly. ✅
- **(n–q)** Eval integrity N/A — no measurement. ✅
- **(t)** Target-gated kill (F#666) **does NOT apply** — F#666 is the *reason* for the preempt, not a blocker; no KC measured. Explicitly carved out in reviewer.md F#666-pure-standalone clause. ✅
- **(u)** Scope-changing fixes N/A — graceful-failure stub is canonical preempt artifact, not scope change. ✅
- **(r)** Prediction-vs-measurement table present (5 rows, all "Not measured"). ✅
- **(s)** Five-lemma proof internally consistent; §L5 cleanly distinguishes from F#669-family, template-regression, proxy-only-lineage-inheritance, cross-paper-combined-loss, and the F#719 novel-mechanism+hygiene pairing memory (F#719 is sibling, not parent → no template-regression). ✅

## Distinctions — sibling vs parent
F#719 `exp_hedgehog_loss_variant_kl_div` is a **sibling** (both = Hedgehog loss-variant-ablation sub-type), NOT a parent. DB `depends_on=[]`. The strictly-weaker KC design of this experiment vs F#719 does **not** constitute template-regression — which requires a formal `depends_on` edge and stripped-caveat inheritance per F#705/F#708/F#709 precedent.

## DB/finding state
- DB status already `killed`; K1872 `[✗]`; evidence row filed.
- F#720 verified at `experiment finding-list` (tail row present).
- Hygiene (`success_criteria`, `references`, `platform` INCOMPLETE) left as-is — F#702 patch path unavailable (4th confirmation); non-blocking for preempt-KILL verdict per F#716 precedent.

## Hard-defer interaction
Analyst F#719 advisory (hard-defer further Hedgehog-framework design-locks) not violated — preempt-KILL **rejects** the design-lock; 7-design-lock pile unchanged.

## Assumptions
- Cos-sim between attention maps is a proxy under guardrail 1007 regardless of directional variation — all interpretations fail L1 identically.
- F#477 collapse-regime applicability to Hedgehog MSE adapters is the default pending any Hedgehog `_impl` measurement; L2 is preempt-time structural reasoning, unaffected by future `_impl` evidence.
- F#719 classified as sibling-with-weaker-KC, not parent — no `depends_on` edge; avoids template-regression fire.

## Routing
Emit `review.killed` → analyst.
