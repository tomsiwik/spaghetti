# LEARNINGS.md — exp_hedgehog_loss_variant_mse

## Core Finding
**KILL (preempt-structural, 3rd triple-fire precedent, F#720).** Sole KC K1872 is cos-sim < 0.70 inter-variant MSE-vs-cos-loss (pure F#666-violation); `depends_on=[]`; 3 hygiene defects. F#666-pure primary (13th drain-window instance, **1st cos-sim-bucket → 6th bucket opens**) + §5 tautological-inter-variant-delta secondary (**7th §5 instance, 1st `intra-loss-function-delta` sub-variant**) + hygiene-multi-defect tertiary (**4th F#702-unavailability confirmation**, post-promotion). No code ran; preempt-structural graceful-failure stub writes results.json.

## Why
Three independent structural clauses compose; each alone is sufficient for KILL. F#720 is the 3rd triple-fire (after F#714 inter-training §5, F#716 intra-adapter-rank §5), extending hierarchy axis-invariance to a **4th distinct §5 sub-variant** (intra-loss-function-delta). F#719 sibling is Hedgehog loss-variant-ablation sub-type with **paired target KC K1871** — KC design (paired vs pure-proxy) cleanly bifurcates verdict at the preempt gate, independent of loss-variant identity (MSE vs KL-div). F#719 is sibling-with-weaker-KC (no `depends_on` edge) → NOT template-regression.

## Implications for Next Experiment
1. **Triple-fire-mode standalone memory promotion DEFERRED to 4th instance.** Hierarchy is already canonically formalized in two places (§5 8th checklist + F#702-impossibility Corollary); 3-instance axis-invariance across 4 sub-variants is strong evidence but promotion adds duplication without marginal Response delta. Promote at 4th triple-fire OR structural divergence between sub-variants.
2. **Hedgehog loss-variant-ablation sub-type ledger: 2 instances** (F#719 paired→PROVISIONAL; F#720 pure-proxy→KILL). Promote sub-type pattern memory at 3rd instance OR if a 3rd Hedgehog framework-level ablation (optimizer, schedule, rank) appears with the paired-vs-proxy bifurcation.
3. **Hard-defer Hedgehog-framework design-locks UNAFFECTED** — preempt-KILL is rejection, 7-design-lock pile remains at 7.
4. **Cos-sim bucket (6th F#666-pure bucket) added at 1-instance** — not confirmed-recurrent; no taxonomy refactor. Promote to recurrent at 2nd cos-sim-only F#666-pure. Bucket list now {derived-geometric(4), summary-distributional(3 saturated), detection/classification(2), routing(3 saturated), infrastructure-benchmark(1), **cos-sim(1)**}. Note: cos-sim is a derived-geometric sub-variant; consider merging under derived-geometric if 2nd cos-sim-only instance appears (vs multi-proxy derived-geometric like F#714).
5. **Unblock path for v2 filed in PAPER.md** — pair inter-variant cos-sim with behavioral-quality target on F#683 politeness corpus; transitive blocker on F#683 `_impl`.
6. **26B teacher cache** remains a standalone prereq task candidate — blocks 8+ Hedgehog dependents.
