.env
__pycache__/
*.pyc
.worktrees/

# Large model files (keep local)
data/adapters/*/adapter_model.safetensors
data/adapters/*/tokenizer.json
data/adapters/*/tokenizer_config.json
*.safetensors
*.gguf
*.bin
*.npz
*.pt
*.pth
*.h5
*.onnx
*.pkl
*.pickle

# Training data (keep local)
data/corpora/distillation/*/train.jsonl
data/corpora/distillation/*/train_split.jsonl
data/corpora/distillation/*/val.jsonl

# Training datasets (reproducible via HF datasets / scripts)
*.parquet
*.jsonl
experiments/**/data/
results/**/data/
results/**/checkpoint-*/

# Results data (keep local)
results/*.jsonl
results/*.csv

# SQLite transient files (never commit)
*.db-wal
*.db-shm

# Large experiment results (some contain full hidden states/embeddings)
experiments/**/results.json

# Paper2code work directory
.paper2code_work/

# OCR traineddata
eng.traineddata

# Screenshots
screenshots/

# SQLite databases
*.db

# Node modules
node_modules/
bun.lock

# Ralph runtime state
.ralph/loop.lock
.ralph/agent/
.ralph/events-*.jsonl
.ralph/current-events
.ralph/current-loop-id
.ralph/diagnostics/
# uv.lock — committed for reproducible installs
.archive/
*.npy

# graphify knowledge-graph outputs (regenerable)
graphify-out/
**/graphify-out/
