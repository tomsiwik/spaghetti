1. **Axiom Inversion:** The field assumes the base model anchors adapters, but what if the base model acts as a damping field, and projecting the interference gap into its null space allows adapters to compute a hidden, parallel forward pass entirely out of their collisions?
2. **Relocation (Depth):** Tracking the derivative of the gap magnitude across transformer layers (dG/dDepth) analytically pinpoints the exact phase transition where memorization becomes reasoning, enabling dynamic layer-truncation based purely on geometric disagreement rather than token probability.
3. **Distant Primitive (Topology):** Treating the pairwise gap matrix of N experts as a persistent homology space reveals that topological "holes" (Betti numbers) in the interference structure map directly to emergent semantic capabilities absent in both the base model and individual experts.
4. **Impossibility Theorem:** It is mathematically impossible to linearly compose correlated experts without degradation unless their activations are disjoint in the frequency domain, proving that spatial magnitude-pruning methods (TIES, DARE) are fundamentally flawed approximations of spectral orthogonality.
5. **Relocation (Decode Step):** Transient spikes in the interference gap at step 't' during autoregressive generation are not collisions to smooth over, but analytic triggers that should forcefully halt decoding to inject a latent reasoning token, using geometric uncertainty as a native hallucination-detector.
6. **Axiom Inversion:** The field assumes catastrophic forgetting is caused by weight-overwriting, but what if forgetting is simply the *collapse of the interference gap*, meaning we can perform surgical unlearning of harmful concepts by deliberately driving their specific expert-gaps to zero?
7. **Distant Primitive (Thermodynamics):** The entropy of the interference pattern across a batch forms a strict informational invariant, providing a mathematically unbreakable threshold that proves exactly when adding an Nth expert will yield zero marginal utility before any forward passes occur.
8. **Impossibility Theorem:** Any continuous optimization process that successfully drives the composition gap `g` to zero will strictly destroy the model's out-of-distribution capabilities, proving that "perfectly merged" models are mathematically isomorphic to perfectly overfit representations.