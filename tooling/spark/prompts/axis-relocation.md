Problem:

{{problem}}

The usual approaches vary the solution along the OBVIOUS axis — the task/domain, the value, the spatial index, the parameter. Relocate the core mechanism onto a DIFFERENT axis it is never placed on: depth / iteration count, time / version, decode-step, frequency, scale, or recursion. Re-index the primitive by that new axis and work out what becomes possible that the obvious axis forbade — and what new control knob you gain. Propose the approach. A few sentences.
